{
  "version": 2,
  "buildCommand": "pnpm install && pnpm db:generate && pnpm build",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
