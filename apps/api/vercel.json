{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install --filter=api... && cd apps/api && pnpm db:generate && pnpm build",
  "installCommand": "cd ../.. && pnpm install --filter=api...",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
