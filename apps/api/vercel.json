{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && pnpm prisma generate --schema=apps/api/prisma/schema.prisma && pnpm turbo build --filter=api",
  "devCommand": "pnpm dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
